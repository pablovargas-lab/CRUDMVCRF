@model CRUDMVCRF.Models.ViewModel.PeopleViewModel
@{
    ViewBag.Title = "New";
}

<h2>New</h2>

<form>

    <div class="row">
        <div class="form-group">
            @Html.LabelFor(d => d.Name)
            @Html.TextBoxFor(d => d.Name)

        </div>
        <div class="form-group">
            @Html.LabelFor(d => d.Age)
            @Html.TextBoxFor(d => d.Age)

        </div>

    </div>
</form>